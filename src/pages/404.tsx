import * as React from 'react'

import {Frame, Fonts} from '../components/layout'

export default () => (
  <Frame>
    <Fonts families={['plex']} />
    <h1>404</h1>
    <p>We looked. It’s not there.</p>
  </Frame>
)
